version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/localhost/htdocs
    restart: unless-stopped

  lighttpd:
    image: sebp/lighttpd
    volumes:
      - .:/var/www/localhost/htdocs/
    depends_on:
      - app
    ports:
      - "9880:80"
    tty: true